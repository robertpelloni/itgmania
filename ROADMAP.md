# Roadmap
1. Port core utility functions (like `sm_crash`) to Rust.
2. Port data types (e.g., strings, math) to Rust.
3. Migrate major components one by one, keeping C++ header bindings.
4. Replace C++ UI handling with native Rust handling (long term).
