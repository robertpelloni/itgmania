# DEPLOY.md

## Deployment Instructions

* Build system: CMake
* Fetch dependencies using `fetch-extern-deps.sh`.
* Ensure necessary libraries (OpenGL, ALSA, PulseAudio, LibUSB, X11 dev headers, GTK3) are installed based on the OS.
* Follow specific OS guidelines in `README.md`.
