# Memory
Initial state: The project is built using CMake and requires numerous external dependencies (which were fetched successfully). A rust_core crate will be integrated into the CMake build process.
